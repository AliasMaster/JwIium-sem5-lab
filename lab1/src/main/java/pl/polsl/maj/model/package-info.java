/**
 * Model package provides matrix abstractions and implementations. It
 * contains the {@link pl.polsl.maj.model.IMatrix} contract and concrete
 * implementations such as {@link pl.polsl.maj.model.Matrix} with common
 * operations (determinant, inverse, multiply, transpose, etc.).
 *
 * @author piotr.maj
 * @version 1.0.0
 */
package pl.polsl.maj.model;
