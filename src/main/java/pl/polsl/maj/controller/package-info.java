/**
 * Controller package contains the application controller which coordinates
 * interactions between the view and the matrix model. The controller parses
 * input, validates data and delegates operations to the model.
 *
 * <p>Primary class: {@link pl.polsl.maj.controller.MatrixController}</p>
 *
 * @author piotr.maj
 * @version 1.0.2
 */
package pl.polsl.maj.controller;
